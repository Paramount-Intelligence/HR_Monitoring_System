/** Canonical production backend URLs for PIMS mobile (override via EXPO_PUBLIC_* at build time). */
module.exports = {
  PRODUCTION_API_BASE_URL:
    'https://hrmonitoringsystem-production.up.railway.app/api/v1',
  PRODUCTION_WS_BASE_URL:
    'wss://hrmonitoringsystem-production.up.railway.app/api/v1/ws',
};
