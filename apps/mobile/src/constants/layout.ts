import { layout } from './theme';

/** Extra bottom padding so scroll content clears the tab bar on tab screens. */
export const TAB_SCREEN_BOTTOM_INSET = layout.tabBarHeight + layout.scrollBottomInset;

/** Minimum pressable size for accessibility. */
export const MIN_TOUCH_TARGET = layout.touchTargetMin;
